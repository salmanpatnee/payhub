---
phase: 1
slug: foundation
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-02
---

# Phase 1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Pest PHP v4.x |
| **Config file** | `phpunit.xml` (generated by Laravel starter kit) |
| **Quick run command** | `php artisan test --filter=EncryptionRoundTripTest` |
| **Full suite command** | `php artisan test` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `php artisan test --filter=EncryptionRoundTripTest`
- **After every plan wave:** Run `php artisan test`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| encryption-secret-key | schema | 1 | SEC-01 | DB dump exposes Stripe keys | `secret_key` stored as ciphertext, not plaintext | DB/Feature | `php artisan test --filter=EncryptionRoundTripTest` | ❌ W0 | ⬜ pending |
| encryption-webhook-secret | schema | 1 | SEC-01 | DB dump exposes Stripe keys | `webhook_secret` stored as ciphertext, not plaintext | DB/Feature | `php artisan test --filter=EncryptionRoundTripTest` | ❌ W0 | ⬜ pending |
| amount-integer-integrity | schema | 1 | SC-5 (ROADMAP) | Float amount causes underpayment | `amount` stored and retrieved as integer cents | DB/Unit | `php artisan test --filter=PaymentAmountIntegrityTest` | ❌ W0 | ⬜ pending |
| model-relationships | models | 1 | SC-7 (ROADMAP) | — | Brand→StripeAccount→Payment relationships traversable | DB/Feature | `php artisan test --filter=ModelRelationshipsTest` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/Feature/EncryptionRoundTripTest.php` — SEC-01: `secret_key` and `webhook_secret` encrypt on save, decrypt on read
- [ ] `tests/Feature/ModelRelationshipsTest.php` — SC-7: Brand→StripeAccount, Payment→Brand, Payment→User relationships
- [ ] `tests/Feature/PaymentAmountIntegrityTest.php` — SC-5: amount stored as integer cents, no float drift

*These files must be created as part of Wave 1 execution. Pest PHP and Laravel's test infrastructure are already installed by the starter kit — no additional test framework installation required.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `php artisan serve` starts without errors | SC-1 (ROADMAP) | Process start is environment-dependent | Run `php artisan serve`, confirm no error output, check http://localhost:8000 |
| `npm run dev` starts and browser shows Tailwind styles | SC-1 (ROADMAP) | Browser render requires visual inspection | Run `npm run dev`, open browser, hard-refresh, confirm at least one Tailwind utility is visibly applied (background, border, or text color) |
| shadcn-vue components.json has `new-york-v4` style | SC-3 (ROADMAP) | JSON file inspection | `cat components.json` — confirm `"style": "new-york-v4"` |
| Seeder runs idempotently twice | SC-8 (ROADMAP) | Requires two consecutive `migrate:fresh --seed` runs | Run `php artisan migrate:fresh --seed` twice; confirm second run produces no duplicate-key errors |
| Tinker relationship traversal | SC-7 (ROADMAP) | Interactive REPL — cannot be automated in CI | Run `php artisan tinker`, execute `Brand::first()->stripeAccounts`, confirm non-empty result |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending

---
phase: 07-notifications-dashboard
plan: "03"
type: execute
wave: 2
depends_on:
  - "07-02"
files_modified:
  - resources/js/pages/payments/Index.vue
autonomous: false
requirements:
  - DASH-02
  - DASH-04

must_haves:
  truths:
    - "A filter bar with brand (admin-only), account (admin-only), status, from-date, and to-date controls appears above the payments table"
    - "Changing any filter triggers an Inertia router.get() with preserveState:true, replace:true — no Apply button needed"
    - "Filter inputs are pre-populated from the 'filters' prop on every page load and back-navigation"
    - "Admin-only filters (brand, account) are hidden for non-admin users via v-if='isAdmin'"
    - "A 'Clear filters' ghost button with X icon appears only when at least one filter has a value"
    - "Empty state shows 'No payments match your filters. Clear filters to see all.' when filters active and 0 results"
  artifacts:
    - path: "resources/js/pages/payments/Index.vue"
      provides: "Extended Index.vue with filter bar UI"
      contains: "router.get"
  key_links:
    - from: "resources/js/pages/payments/Index.vue filter bar"
      to: "payments.index route"
      via: "router.get(route('payments.index'), activeFilters, { preserveState: true, replace: true })"
      pattern: "router.get"
    - from: "filters reactive state"
      to: "props.filters"
      via: "reactive({ ...props.filters })"
      pattern: "reactive.*props.filters"
---

<objective>
Add a filter bar to `payments/Index.vue` above the existing table. The filter bar uses shadcn-vue `<Select>` components for brand/account/status and native `<input type="date">` (via `<Input>`) for date range. Every filter change auto-submits via Inertia `router.get()`. A "Clear filters" ghost button appears when any filter is active.

Purpose: Fulfil DASH-02 (filter UI) with the auto-submit behaviour decided in D-07. The backend filter logic is already live from plan 07-02 — this plan wires the Vue UI to it.
Output: Modified `payments/Index.vue`; visual filter bar renders correctly; checkpoint verifies browser behaviour.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-notifications-dashboard/07-CONTEXT.md
@.planning/phases/07-notifications-dashboard/07-RESEARCH.md
@.planning/phases/07-notifications-dashboard/07-UI-SPEC.md
@.planning/phases/07-notifications-dashboard/07-02-SUMMARY.md

<interfaces>
<!-- Current payments/Index.vue defineProps — will be extended -->
```typescript
// CURRENT (plan 07-02 did not touch the Vue file):
defineProps<{ payments: PaymentRow[] }>();

// MUST BECOME:
type FilterState = {
    brand_id: string;
    stripe_account_id: string;
    status: string;
    from: string;
    to: string;
};

defineProps<{
    payments: PaymentRow[];
    filters: FilterState;
    brands: { id: number; name: string }[];
    accounts: { id: number; account_name: string }[];
    isAdmin: boolean;
}>();
```

<!-- Inertia router.get() signature (Inertia v3) -->
```typescript
import { router } from '@inertiajs/vue3';
router.get(url: string, data?: object, options?: { preserveState?: boolean; replace?: boolean }): void
```

<!-- shadcn-vue Select component import pattern (from resources/js/pages/payments/Create.vue) -->
```typescript
import {
    Select,
    SelectContent,
    SelectItem,
    SelectTrigger,
    SelectValue,
} from '@/components/ui/select';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
```

<!-- Lucide X icon (new for this phase — all other icons already imported) -->
```typescript
import { Check, Copy, Eye, Plus, X } from 'lucide-vue-next';
```

<!-- route() function — already used in existing copyLink -->
```typescript
import { route } from 'ziggy-js';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add filter bar to payments/Index.vue</name>
  <files>resources/js/pages/payments/Index.vue</files>

  <read_first>
    - resources/js/pages/payments/Index.vue — READ the entire current file; note all imports, defineProps, defineOptions, existing functions (formatAmount, statusClass, copyLink), and the template structure
    - resources/js/pages/payments/Create.vue — extract the exact Select/SelectContent/SelectItem/SelectTrigger/SelectValue import pattern used in this project
    - .planning/phases/07-notifications-dashboard/07-UI-SPEC.md — filter bar layout spec (flex items-center gap-3 flex-wrap), component inventory, copywriting contract (placeholder text), accessible interaction notes, interaction contract (auto-submit, clear filters button)
    - .planning/phases/07-notifications-dashboard/07-RESEARCH.md — Pattern 6 (full Vue filter bar code with router.get(), reactive(), watch(), FilterState type)
    - .planning/phases/07-notifications-dashboard/07-CONTEXT.md — D-05 (admin-only filters: brand, account), D-06 (server-side Inertia), D-07 (auto-submit on change, no Apply button)
  </read_first>

  <action>
Rewrite `resources/js/pages/payments/Index.vue` completely. Keep all existing logic (formatAmount, statusClass, copyLink, defineOptions, existing table template) and add the filter bar.

**`<script setup lang="ts">` section — additions:**

1. Add to imports:
   - `router` from `@inertiajs/vue3` (add to existing `Head, Link` import line or add separately)
   - `reactive, watch, computed` from `vue` (currently only `ref` is imported — extend)
   - `route` from `ziggy-js`
   - `X` to the existing lucide imports (add to `Check, Copy, Eye, Plus`)
   - `Select, SelectContent, SelectItem, SelectTrigger, SelectValue` from `@/components/ui/select`
   - `Input` from `@/components/ui/input`
   - `Label` from `@/components/ui/label`

2. Add `FilterState` type:
```typescript
type FilterState = {
    brand_id: string;
    stripe_account_id: string;
    status: string;
    from: string;
    to: string;
};
```

3. Replace `defineProps<{ payments: PaymentRow[] }>()` with:
```typescript
const props = defineProps<{
    payments: PaymentRow[];
    filters: FilterState;
    brands: { id: number; name: string }[];
    accounts: { id: number; account_name: string }[];
    isAdmin: boolean;
}>();
```

4. Add after defineProps:
```typescript
const filters = reactive<FilterState>({ ...props.filters });

const hasActiveFilters = computed(() =>
    Object.values(filters).some((v) => v !== '')
);

watch(
    filters,
    (newFilters) => {
        router.get(
            route('payments.index'),
            Object.fromEntries(Object.entries(newFilters).filter(([, v]) => v !== '')),
            { preserveState: true, replace: true },
        );
    },
    { deep: true },
);

function clearFilters(): void {
    filters.brand_id = '';
    filters.stripe_account_id = '';
    filters.status = '';
    filters.from = '';
    filters.to = '';
}
```

**`<template>` section — filter bar:**

Insert the filter bar `<div>` between the page header `<div>` (containing h1 + "New payment" Button) and the table card `<div>`. The filter bar must have `class="mb-4"` (16px gap from table).

Filter bar template:
```html
<!-- Filter bar — appears between page header and table card -->
<div class="flex items-center gap-3 flex-wrap mb-4">
    <!-- Brand filter — admin only (D-05) -->
    <div v-if="isAdmin" class="flex flex-col gap-1">
        <Label class="text-xs text-muted-foreground">Brand</Label>
        <Select v-model="filters.brand_id">
            <SelectTrigger class="w-40">
                <SelectValue placeholder="All brands" />
            </SelectTrigger>
            <SelectContent>
                <SelectItem value="">All brands</SelectItem>
                <SelectItem
                    v-for="brand in brands"
                    :key="brand.id"
                    :value="String(brand.id)"
                >
                    {{ brand.name }}
                </SelectItem>
            </SelectContent>
        </Select>
    </div>

    <!-- Stripe account filter — admin only (D-05) -->
    <div v-if="isAdmin" class="flex flex-col gap-1">
        <Label class="text-xs text-muted-foreground">Account</Label>
        <Select v-model="filters.stripe_account_id">
            <SelectTrigger class="w-44">
                <SelectValue placeholder="All accounts" />
            </SelectTrigger>
            <SelectContent>
                <SelectItem value="">All accounts</SelectItem>
                <SelectItem
                    v-for="account in accounts"
                    :key="account.id"
                    :value="String(account.id)"
                >
                    {{ account.account_name }}
                </SelectItem>
            </SelectContent>
        </Select>
    </div>

    <!-- Status filter — all roles -->
    <div class="flex flex-col gap-1">
        <Label class="text-xs text-muted-foreground">Status</Label>
        <Select v-model="filters.status">
            <SelectTrigger class="w-36">
                <SelectValue placeholder="All statuses" />
            </SelectTrigger>
            <SelectContent>
                <SelectItem value="">All statuses</SelectItem>
                <SelectItem value="pending">Pending</SelectItem>
                <SelectItem value="completed">Completed</SelectItem>
                <SelectItem value="failed">Failed</SelectItem>
                <SelectItem value="cancelled">Cancelled</SelectItem>
            </SelectContent>
        </Select>
    </div>

    <!-- From date — all roles -->
    <div class="flex flex-col gap-1">
        <Label class="text-xs text-muted-foreground">From</Label>
        <Input v-model="filters.from" type="date" class="w-36" />
    </div>

    <!-- To date — all roles -->
    <div class="flex flex-col gap-1">
        <Label class="text-xs text-muted-foreground">To</Label>
        <Input v-model="filters.to" type="date" class="w-36" />
    </div>

    <!-- Clear filters — visible only when any filter active -->
    <div v-if="hasActiveFilters" class="flex flex-col justify-end">
        <Button
            variant="ghost"
            size="sm"
            aria-label="Clear all filters"
            @click="clearFilters"
        >
            <X class="size-4 mr-1" />
            Clear filters
        </Button>
    </div>
</div>
```

**Empty state update** — replace the existing empty state `<tr>`:
```html
<tr v-if="payments.length === 0">
    <td colspan="8" class="px-4 py-12 text-center text-muted-foreground text-sm">
        <template v-if="hasActiveFilters">
            No payments match your filters.
            <button class="underline" @click="clearFilters">Clear filters to see all.</button>
        </template>
        <template v-else>
            No payments yet. <Link href="/payments/create" class="underline">Create one.</Link>
        </template>
    </td>
</tr>
```

**Important:** `filters.brand_id` and `filters.stripe_account_id` must be stored as strings in the reactive state (not numbers) to match what `<Select v-model>` produces. The backend `->when()` coerces the string to an integer via Eloquent's parameterised query.
  </action>

  <verify>
    <automated>php artisan test --compact 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep "router.get" resources/js/pages/payments/Index.vue` matches
    - `grep "preserveState: true" resources/js/pages/payments/Index.vue` matches
    - `grep "replace: true" resources/js/pages/payments/Index.vue` matches
    - `grep "reactive.*props.filters" resources/js/pages/payments/Index.vue` matches
    - `grep "hasActiveFilters" resources/js/pages/payments/Index.vue` matches (computed + v-if usage)
    - `grep "v-if=\"isAdmin\"" resources/js/pages/payments/Index.vue` matches (admin-only filter wrappers)
    - `grep "clearFilters" resources/js/pages/payments/Index.vue` matches (function + button usage)
    - `grep "aria-label=\"Clear all filters\"" resources/js/pages/payments/Index.vue` matches
    - `grep "No payments match your filters" resources/js/pages/payments/Index.vue` matches
    - `grep "Select\|SelectContent\|SelectItem\|SelectTrigger" resources/js/pages/payments/Index.vue` matches
    - `php artisan test --compact` exits 0 (full suite GREEN — no regressions)
  </acceptance_criteria>

  <done>Filter bar rendered in Index.vue with all 5 filter controls, auto-submit watcher, clear button, updated empty state. Full test suite GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Filter bar UI in payments/Index.vue:
    - Brand and Account dropdowns (admin only, hidden for User role)
    - Status dropdown (all roles)
    - From/To date inputs (all roles)
    - Auto-submit on every filter change (no Apply button)
    - "Clear filters" button appears when any filter is active
    - Empty state message changes when filters active and no results
    - Filter values preserved on page back-navigation
  </what-built>

  <how-to-verify>
    1. Start the dev server: `composer run dev` (or `php artisan serve` + `npm run dev` in separate terminals)
    2. Log in as an Admin user at `http://localhost:8000/login`
    3. Navigate to `http://localhost:8000/payments`
    4. Confirm the filter bar appears above the payments table with: Brand dropdown, Account dropdown, Status dropdown, From date, To date
    5. Change the Status dropdown to "Completed" — confirm the URL changes to `?status=completed` and the list filters WITHOUT a full page reload (Inertia SPA navigation)
    6. Apply a brand filter — confirm the list narrows to that brand's payments
    7. Clear filters button: confirm it appears when any filter has a value, and clicking it resets all filters and returns to full list
    8. Apply a filter, navigate to a payment detail page, press Back — confirm filter values are restored in the filter bar
    9. Log out and log in as a User (non-admin) — navigate to /payments — confirm Brand and Account dropdowns are NOT visible; only Status + date range appear
    10. Confirm empty state text: if filters are active with no results, shows "No payments match your filters. Clear filters to see all." If no filters and no results, shows "No payments yet. Create one."
  </how-to-verify>

  <resume-signal>Type "approved" to complete Phase 7, or describe any issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vue component → Inertia router.get() | Filter state flows from reactive UI state to URL query params |
| URL query params → PaymentController | Handled in plan 07-02; no new trust boundary introduced here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Elevation of Privilege | Admin-only filter v-if | accept | `v-if="isAdmin"` hides admin-only filter inputs client-side. Server-side enforcement is in PaymentController (plan 07-02) — the non-admin user_id scope prevents data leakage even if client-side guard is bypassed. Defense-in-depth: both layers enforce role separation. |
| T-07-03-02 | Tampering | filter reactive state | accept | Filter values are strings bound to shadcn-vue Select (value from predefined SelectItem set) or native date inputs. No free-form string injection path — values are URL-encoded query params processed via Eloquent parameterised queries on the server. |
</threat_model>

<verification>
After the checkpoint is approved:

```bash
php artisan test --compact
```

Expected: Full suite GREEN (all prior tests + NotificationTest + PaymentDashboardTest).

Manual verification checklist (completed during checkpoint):
- [ ] Filter bar renders above table on /payments
- [ ] Admin sees all 5 filter controls (brand, account, status, from, to)
- [ ] User sees only 3 filter controls (status, from, to)
- [ ] Auto-submit works (URL updates without full reload)
- [ ] Clear filters button appears/disappears correctly
- [ ] Filter state persists on back-navigation
- [ ] Empty state messages display correctly based on filter state
</verification>

<success_criteria>
- Filter bar renders in the correct position (above table card, below page header) with `mb-4` spacing
- All 5 filter controls render with correct placeholder text per UI-SPEC copywriting contract
- `v-if="isAdmin"` correctly gates brand and account dropdowns
- `watch(filters, ..., { deep: true })` triggers `router.get()` with `preserveState: true, replace: true` on every change
- `hasActiveFilters` computed shows/hides clear button correctly
- Empty state text adapts based on `hasActiveFilters`
- `php artisan test --compact` exits 0
- Checkpoint approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/07-notifications-dashboard/07-03-SUMMARY.md`
</output>

---
phase: 03-brand-stripe-account-management
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/Feature/Admin/BrandManagementTest.php
  - tests/Feature/Admin/StripeAccountManagementTest.php
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03
  - BRAND-04
  - STRIPE-01
  - STRIPE-03
  - STRIPE-04
  - STRIPE-05

must_haves:
  truths:
    - "BrandManagementTest.php exists with stubs for list, create, update, and non-admin-blocked tests"
    - "StripeAccountManagementTest.php exists with stubs for list, create, update, deactivate, test-key-blocked, and key-validation tests"
    - "Both test files run with php artisan test and all stubs either pass (skipped/incomplete) or fail with a clear reason — not a syntax error or missing class"
    - "Both test classes include the spatie permission setUp() + adminUser() helper"
  artifacts:
    - path: "tests/Feature/Admin/BrandManagementTest.php"
      provides: "Brand CRUD test stubs covering BRAND-01, BRAND-02, BRAND-03"
      contains: "class BrandManagementTest"
    - path: "tests/Feature/Admin/StripeAccountManagementTest.php"
      provides: "Stripe account test stubs covering BRAND-04, STRIPE-01, STRIPE-03, STRIPE-04, STRIPE-05"
      contains: "class StripeAccountManagementTest"
  key_links:
    - from: "tests/Feature/Admin/BrandManagementTest.php"
      to: "tests/Feature/Auth/AdminUserManagementTest.php"
      via: "setUp() + adminUser() pattern copied exactly"
      pattern: "forgetCachedPermissions"
    - from: "tests/Feature/Admin/StripeAccountManagementTest.php"
      to: "tests/Feature/Auth/AdminUserManagementTest.php"
      via: "setUp() + adminUser() pattern copied exactly"
      pattern: "forgetCachedPermissions"
---

<objective>
Create Wave 0 test stubs for Phase 3 so that the Nyquist sampling contract is satisfied before any backend implementation begins. Every backend task in Waves 1+ references these test files for automated verification.

Purpose: The VALIDATION.md contract requires Wave 0 stubs to exist before any Wave 1 implementation. Stubs define the test contract upfront; Wave 1 implementation makes them green.

Output: Two test files with complete test method stubs (all assert calls written, bodies marked `$this->markTestIncomplete()`), both runnable without syntax errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-brand-stripe-account-management/03-CONTEXT.md
@.planning/phases/03-brand-stripe-account-management/03-VALIDATION.md
</context>

<interfaces>
<!-- Existing test infrastructure — copy setUp() + adminUser() exactly from this class -->
From tests/Feature/Auth/AdminUserManagementTest.php:
```php
protected function setUp(): void
{
    parent::setUp();
    app()[\Spatie\Permission\PermissionRegistrar::class]->forgetCachedPermissions();
    Role::firstOrCreate(['name' => 'admin', 'guard_name' => 'web']);
    Role::firstOrCreate(['name' => 'user',  'guard_name' => 'web']);
}

private function adminUser(): User
{
    $admin = User::factory()->create(['email_verified_at' => now()]);
    $admin->syncRoles(['admin']);
    return $admin;
}
```

From app/Models/Brand.php:
```php
protected $fillable = ['name', 'slug', 'logo_path', 'primary_color', 'secondary_color'];
public function stripeAccounts(): HasMany
```

From app/Models/StripeAccount.php:
```php
protected $fillable = ['brand_id', 'account_name', 'publishable_key', 'is_active'];
// secret_key NOT mass-assignable
protected function casts(): array { return ['secret_key' => 'encrypted', 'is_active' => 'boolean']; }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create BrandManagementTest stubs (BRAND-01, BRAND-02, BRAND-03)</name>
  <files>tests/Feature/Admin/BrandManagementTest.php</files>

  <read_first>
    - tests/Feature/Auth/AdminUserManagementTest.php — copy setUp() and adminUser() EXACTLY (lines 14-27)
    - app/Models/Brand.php — confirm fillable fields and stripeAccounts() relationship name
  </read_first>

  <action>
Create `tests/Feature/Admin/BrandManagementTest.php` with the following complete structure:

```php
<?php

namespace Tests\Feature\Admin;

use App\Models\Brand;
use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Http\UploadedFile;
use Illuminate\Support\Facades\Storage;
use Spatie\Permission\Models\Role;
use Tests\TestCase;

class BrandManagementTest extends TestCase
{
    use RefreshDatabase;

    protected function setUp(): void
    {
        parent::setUp();
        app()[\Spatie\Permission\PermissionRegistrar::class]->forgetCachedPermissions();
        Role::firstOrCreate(['name' => 'admin', 'guard_name' => 'web']);
        Role::firstOrCreate(['name' => 'user',  'guard_name' => 'web']);
    }

    private function adminUser(): User
    {
        $admin = User::factory()->create(['email_verified_at' => now()]);
        $admin->syncRoles(['admin']);
        return $admin;
    }

    // BRAND-03: Admin can list all brands
    public function test_admin_can_view_brand_list(): void
    {
        $this->markTestIncomplete('Wave 0 stub — implement after Wave 1 backend.');
    }

    // BRAND-01: Admin can create a brand with name, logo, primary/secondary colors
    public function test_admin_can_create_brand_without_logo(): void
    {
        $this->markTestIncomplete('Wave 0 stub — implement after Wave 1 backend.');
    }

    // BRAND-01: Admin can create a brand with logo upload
    public function test_admin_can_create_brand_with_logo(): void
    {
        $this->markTestIncomplete('Wave 0 stub — implement after Wave 1 backend.');
    }

    // BRAND-01: Slug is auto-generated from name (not user-inputted)
    public function test_brand_slug_is_auto_generated_from_name(): void
    {
        $this->markTestIncomplete('Wave 0 stub — implement after Wave 1 backend.');
    }

    // BRAND-02: Admin can edit brand details
    public function test_admin_can_update_brand(): void
    {
        $this->markTestIncomplete('Wave 0 stub — implement after Wave 1 backend.');
    }

    // BRAND-02: On edit with new logo, old logo file is deleted from storage
    public function test_old_logo_deleted_when_new_logo_uploaded(): void
    {
        $this->markTestIncomplete('Wave 0 stub — implement after Wave 1 backend.');
    }

    // BRAND-02: On edit without new logo, existing logo_path is preserved
    public function test_logo_preserved_when_no_new_logo_submitted(): void
    {
        $this->markTestIncomplete('Wave 0 stub — implement after Wave 1 backend.');
    }

    // Access control: non-admin cannot access brand routes
    public function test_non_admin_cannot_access_brand_list(): void
    {
        $this->markTestIncomplete('Wave 0 stub — implement after Wave 1 backend.');
    }

    // Validation: primary_color must be a valid 6-char hex string
    public function test_primary_color_must_be_valid_hex(): void
    {
        $this->markTestIncomplete('Wave 0 stub — implement after Wave 1 backend.');
    }
}
```

The stubs must use `$this->markTestIncomplete(...)` so Pest/PHPUnit marks them as incomplete (I) rather than failing (F). Do NOT use `$this->assertTrue(false)` or leave bodies empty.
  </action>

  <verify>
    <automated>php artisan test tests/Feature/Admin/BrandManagementTest.php 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - File exists at tests/Feature/Admin/BrandManagementTest.php
    - `grep -c "markTestIncomplete" tests/Feature/Admin/BrandManagementTest.php` returns 9
    - `grep "class BrandManagementTest" tests/Feature/Admin/BrandManagementTest.php` matches
    - `grep "forgetCachedPermissions" tests/Feature/Admin/BrandManagementTest.php` matches
    - `php artisan test tests/Feature/Admin/BrandManagementTest.php` exits 0 (all tests incomplete/skipped, no errors)
    - `grep "namespace Tests\\\\Feature\\\\Admin" tests/Feature/Admin/BrandManagementTest.php` matches
  </acceptance_criteria>

  <done>BrandManagementTest.php exists, runs without errors, all 9 test methods marked incomplete.</done>
</task>

<task type="auto">
  <name>Task 2: Create StripeAccountManagementTest stubs (BRAND-04, STRIPE-01, STRIPE-03, STRIPE-04, STRIPE-05)</name>
  <files>tests/Feature/Admin/StripeAccountManagementTest.php</files>

  <read_first>
    - tests/Feature/Auth/AdminUserManagementTest.php — copy setUp() and adminUser() EXACTLY (lines 14-27)
    - app/Models/StripeAccount.php — confirm fillable fields, casts, and brand() relationship
    - app/Models/Brand.php — confirm factory is available (HasFactory trait present)
  </read_first>

  <action>
Create `tests/Feature/Admin/StripeAccountManagementTest.php` with the following complete structure. Note: STRIPE-02 (encrypted cast) is already covered by the existing `tests/Feature/EncryptionRoundTripTest.php` — do NOT duplicate it here.

```php
<?php

namespace Tests\Feature\Admin;

use App\Models\Brand;
use App\Models\StripeAccount;
use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Spatie\Permission\Models\Role;
use Tests\TestCase;

class StripeAccountManagementTest extends TestCase
{
    use RefreshDatabase;

    protected function setUp(): void
    {
        parent::setUp();
        app()[\Spatie\Permission\PermissionRegistrar::class]->forgetCachedPermissions();
        Role::firstOrCreate(['name' => 'admin', 'guard_name' => 'web']);
        Role::firstOrCreate(['name' => 'user',  'guard_name' => 'web']);
    }

    private function adminUser(): User
    {
        $admin = User::factory()->create(['email_verified_at' => now()]);
        $admin->syncRoles(['admin']);
        return $admin;
    }

    // STRIPE-01: Admin can view stripe accounts for a brand
    public function test_admin_can_view_stripe_accounts_for_brand(): void
    {
        $this->markTestIncomplete('Wave 0 stub — implement after Wave 1 backend.');
    }

    // STRIPE-01: Admin can create a stripe account linked to a brand
    public function test_admin_can_create_stripe_account(): void
    {
        $this->markTestIncomplete('Wave 0 stub — implement after Wave 1 backend.');
    }

    // STRIPE-01: brand_id comes from route binding, not request body
    public function test_brand_id_comes_from_route_not_request(): void
    {
        $this->markTestIncomplete('Wave 0 stub — implement after Wave 1 backend.');
    }

    // STRIPE-04: Admin can update a stripe account (blank secret_key keeps existing)
    public function test_admin_can_update_stripe_account_without_changing_key(): void
    {
        $this->markTestIncomplete('Wave 0 stub — implement after Wave 1 backend.');
    }

    // STRIPE-04: Admin can update a stripe account with a new secret_key
    public function test_admin_can_update_stripe_account_with_new_key(): void
    {
        $this->markTestIncomplete('Wave 0 stub — implement after Wave 1 backend.');
    }

    // STRIPE-05: Admin can deactivate a stripe account (is_active = false, not deleted)
    public function test_admin_can_deactivate_stripe_account(): void
    {
        $this->markTestIncomplete('Wave 0 stub — implement after Wave 1 backend.');
    }

    // STRIPE-05: Deactivated account is not deleted from the database
    public function test_deactivated_account_remains_in_database(): void
    {
        $this->markTestIncomplete('Wave 0 stub — implement after Wave 1 backend.');
    }

    // BRAND-04: Test-mode publishable key is rejected in production environment
    public function test_test_publishable_key_blocked_in_production(): void
    {
        $this->markTestIncomplete('Wave 0 stub — implement after Wave 1 backend.');
    }

    // BRAND-04: Test-mode secret key is rejected in production environment
    public function test_test_secret_key_blocked_in_production(): void
    {
        $this->markTestIncomplete('Wave 0 stub — implement after Wave 1 backend.');
    }

    // STRIPE-03: Invalid key pair is rejected with a stripe_api validation error
    public function test_invalid_stripe_key_pair_is_rejected(): void
    {
        $this->markTestIncomplete('Wave 0 stub — implement after Wave 1 backend (requires StripeClient mock).');
    }

    // Access control: non-admin cannot access stripe account routes
    public function test_non_admin_cannot_access_stripe_accounts(): void
    {
        $this->markTestIncomplete('Wave 0 stub — implement after Wave 1 backend.');
    }

    // Scoped binding: cannot access stripe account from a different brand
    public function test_cannot_access_stripe_account_from_wrong_brand(): void
    {
        $this->markTestIncomplete('Wave 0 stub — implement after Wave 1 backend.');
    }
}
```

All stubs use `$this->markTestIncomplete(...)`. The STRIPE-03 mock stub includes a note about StripeClient mocking in its incomplete message.
  </action>

  <verify>
    <automated>php artisan test tests/Feature/Admin/StripeAccountManagementTest.php 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - File exists at tests/Feature/Admin/StripeAccountManagementTest.php
    - `grep -c "markTestIncomplete" tests/Feature/Admin/StripeAccountManagementTest.php` returns 12
    - `grep "class StripeAccountManagementTest" tests/Feature/Admin/StripeAccountManagementTest.php` matches
    - `grep "forgetCachedPermissions" tests/Feature/Admin/StripeAccountManagementTest.php` matches
    - `php artisan test tests/Feature/Admin/StripeAccountManagementTest.php` exits 0
    - `php artisan test` exits 0 (both new files plus existing suite pass)
  </acceptance_criteria>

  <done>StripeAccountManagementTest.php exists, runs without errors, all 12 stubs marked incomplete. Full suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test files only | No production code in this plan — no trust boundary crossings. Test infrastructure only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-W0-01 | Information Disclosure | Test stubs | accept | Test stubs contain no real credentials, fake data only. `markTestIncomplete()` bodies have no implementation. |
</threat_model>

<verification>
After Wave 0:
- `php artisan test tests/Feature/Admin/BrandManagementTest.php` → exits 0, all tests show as "I" (incomplete)
- `php artisan test tests/Feature/Admin/StripeAccountManagementTest.php` → exits 0, all tests show as "I"
- `php artisan test` → full suite exits 0 (existing passing tests + new incomplete stubs)
- Both files contain `forgetCachedPermissions` (spatie permission setUp pattern)
</verification>

<success_criteria>
Two test stub files exist and run cleanly. The Nyquist sampling contract defined in VALIDATION.md is satisfied — all Wave 1 backend tasks now have test files to run against.
</success_criteria>

<output>
After completion, create `.planning/phases/03-brand-stripe-account-management/03-00-SUMMARY.md`
</output>
